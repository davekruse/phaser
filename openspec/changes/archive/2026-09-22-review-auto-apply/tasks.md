## 1. Reviewer tags auto-applicable findings

- [x] 1.1 In `agents/reviewer.md` `## Return`, after the false-claim paragraph and before "End your reply with exactly this block:", insert a paragraph per D2: "Tag a finding `[auto]` directly after its severity tag only when all five hold: the severity is nit or should-fix; the recommended remedy's con reads `none`; the fix is a single localized edit with one defensible form; you are highly confident; the finding does not name a criterion you marked `not-met`. A blocker never carries `[auto]`. When unsure, withhold the tag — a withheld tag costs one picker, a wrong tag costs an unreviewed edit." Verify: `grep -c "withhold the tag" agents/reviewer.md` = 1 and `grep -c "does not name a criterion you marked" agents/reviewer.md` = 1.
- [x] 1.2 In `agents/reviewer.md` Return code block, change the finding placeholder line `1. [blocker|should-fix|nit] <title> — <file:line>` to `1. [blocker|should-fix|nit] [auto]? <title> — <file:line>`. Verify: `grep -c "\[auto\]" agents/reviewer.md` ≥ 2.

## 2. Review applies them before the walk

- [x] 2.1 In `commands/review.md` Step 3, replace the paragraph "Otherwise present the subagent's numbered summary of all findings, with their severities, first. Then walk through them ONE at a time:" with, wrapped at the file's width: "Otherwise, first apply the recommended remedy of every finding tagged `[auto]`, in numbered order, without asking — these are the reviewer's own no-downside calls, and you report them in first person. Then present the numbered summary of all findings with their severities, marking each auto-applied one "applied". If an `[auto]` remedy cannot be applied as written — the file no longer matches, or the remedy is ambiguous or looks wrong — do not improvise: leave it unapplied, mark it "not auto-applied" in the summary, and walk it like any untagged finding. Then walk the remaining findings ONE at a time; if none remain, skip to Step 4:". Verify: `grep -c "tagged \`\[auto\]\`" commands/review.md` = 1, `grep -c "if none remain, skip to Step 4" commands/review.md` = 1, and `grep -c "not auto-applied" commands/review.md` = 1.
- [x] 2.3 In `commands/review.md` Step 4, the Review notes format currently wraps with its last line reading `` <list|none>; criteria: met <nums|none>; unverifiable <nums|none>`. Copy the `` (or similar). Rewrap it so a new line begins at `unverifiable`, and that line reads `` unverifiable <nums|none>; auto-applied: <n|none>`. `` followed by the rest of the paragraph. Then extend the sentence "Copy the numbers from the subagent's `CRITERIA:` section, adjusted by the re-judge above." with "; `auto-applied` is the count of `[auto]` findings you applied in Step 3." Verify: `grep -c "auto-applied: <n|none>" commands/review.md` = 1 and `grep -c "is the count of \`\[auto\]\` findings" commands/review.md` = 1.

## 3. Decision protocol

- [x] 3.1 Append item 5 to `reference/decision-protocol.md`: "**Do not ask about obvious choices.** When the option you would recommend has no con, your confidence is high, and the action is a single reversible edit, act and report it (first person — it is your call, not the user's). Only steps whose command text adopts this clause use it; today that is `/phaser:review`, driven by the reviewer's `[auto]` tag." Verify: `grep -c "Do not ask about obvious choices" reference/decision-protocol.md` = 1.

## 4. Companion docs and version

- [x] 4.1 In `README.md`, after the numbered list in "How decisions reach you" (the item ending 'The built-in "Other" is always there when none of the options fit.'), add a paragraph: "`review` goes one step further: findings the reviewer marks as having no downside are applied before you are asked anything, and named as applied in the summary — pickers are reserved for real trade-offs." Verify: `grep -c "reserved for real trade-offs" README.md` = 1.
- [x] 4.2 `.claude-plugin/plugin.json` version `0.5.3` → `0.5.4`. Verify: `grep -c '"version": "0.5.4"' .claude-plugin/plugin.json` = 1.
- [x] 4.3 Invariant check: `git diff --quiet HEAD -- commands/plan.md commands/aim.md commands/propose.md commands/scrutinize.md commands/apply.md commands/archive.md commands/stun.md agents/scrutinizer.md agents/implementer.md agents/spec-advisor.md` exits 0. Verify by running it.
