## 1. Reviewer: broadened `[auto]` bar

- [x] 1.1 In `agents/reviewer.md` `## Return`, replace the whole paragraph that begins "Tag a finding `[auto]` directly after its severity tag only when all five" and ends "a withheld tag costs one picker, a wrong tag costs an unreviewed edit." with (wrapped at the file's ~78-column width): "Tag a finding `[auto]` directly after its severity tag, at any severity, only when all three hold: you are highly confident; the recommended remedy's con reads `none` or names only extra work (more edits, more files touched); no alternative remedy is equally good. A finding that names a criterion you marked `not-met` is eligible. When the recommended remedy's only effect is to bring an OpenSpec artifact or spec, the README or another doc, or CLAUDE.md in line with the actual code, and you are confident the code (not the doc) is correct, tag it `[auto]` regardless of its con. Give every `[auto]` finding an `Auto because:` line saying in one line why the choice is obvious. When unsure, withhold the tag — a withheld tag costs one picker, a wrong tag costs an unreviewed edit." Verify: `grep -c "withhold the tag" agents/reviewer.md` = 1, `grep -c "only when all five" agents/reviewer.md` = 0, `grep -c "A blocker never carries" agents/reviewer.md` = 0.
- [x] 1.2 In the `agents/reviewer.md` Return code block, directly after the line `   Impact: <one line>`, insert the line `   Auto because: <one line>   (only on [auto] findings)`. Verify: `grep -c "Auto because:" agents/reviewer.md` = 2 and `grep -c "\[auto\]" agents/reviewer.md` ≥ 3.

## 2. Scrutinizer: adopt `[auto]`

- [x] 2.1 In `agents/scrutinizer.md` `## Return`, after the paragraph ending `"leave as-is".` and before "End your reply with exactly this block:", insert a paragraph (wrapped at ~78 columns): "Tag a finding `[auto]` directly after its `[Question|Decision]` tag only when all three hold: you are highly confident; the recommended option's con reads `none` or names only extra work (more edits, more files touched); no alternative option is equally good. When the recommended option's only effect is to bring an OpenSpec artifact or spec, the README or another doc, or CLAUDE.md in line with the actual code, and you are confident the code (not the doc) is correct, tag it `[auto]` regardless of its con — including every false-claim finding above. A factual correction to the plan phase's "Key code touchpoints" counts as such a sync; an option that edits the phase's Requirements, Constraints / early decisions or Acceptance criteria is never `[auto]`. Give every `[auto]` finding an `Auto because:` line saying in one line why the choice is obvious. When unsure, withhold the tag — a withheld tag costs one picker, a wrong tag costs an unreviewed edit." Verify: `grep -c "withhold the tag" agents/scrutinizer.md` = 1.
- [x] 2.2 In the `agents/scrutinizer.md` Return code block, change `1. [Question|Decision] <title>` to `1. [Question|Decision] [auto]? <title>`, and directly after the line `   Why: <one line>` insert `   Auto because: <one line>   (only on [auto] findings)`. Verify: `grep -c "\[auto\]" agents/scrutinizer.md` ≥ 2 and `grep -c "Auto because:" agents/scrutinizer.md` = 2.

## 3. Review: information points and re-judge

- [x] 3.1 In `commands/review.md` Step 3, in the paragraph beginning "Otherwise, first apply the recommended remedy", replace the sentence "Then present the numbered summary of all findings with their severities, marking each auto-applied one "applied"." with: "Then present the numbered summary of all findings with their severities, marking each auto-applied one "applied" with its `Auto because:` reason. Follow the summary with an information point for each auto-applied blocker and each auto-applied finding that names a `not-met` criterion: a short paragraph naming the finding, the remedy you applied, its `Auto because:` reason, and the alternatives you passed over." Leave the rest of the paragraph unchanged; rewrap it at ~78 columns. Verify: `tr '\n' ' ' < commands/review.md | grep -c 'alternatives you passed over'` = 1 and `tr '\n' ' ' < commands/review.md | grep -c 'not auto-applied'` = 1.
- [x] 3.2 In `commands/review.md` Step 4 "Re-judge after fixes", replace `only if a "Fix now" edit in Step 3 addressed the finding that` with `only if a "Fix now" edit or an auto-applied remedy in Step 3 addressed the finding that`, rewrapping the paragraph at ~78 columns. Verify: `tr '\n' ' ' < commands/review.md | grep -c 'or an auto-applied remedy in Step 3'` = 1.

## 4. Scrutinize: auto pre-walk and Scrutiny notes

- [x] 4.1 In `commands/scrutinize.md` Step 3, replace the paragraph "Otherwise present the subagent's one-line numbered summary of all findings first, so the user knows the shape of the conversation. Then walk through them ONE at a time — never dump the full analysis at once." with (wrapped at ~78 columns): "Otherwise, first take the recommended option of every finding tagged `[auto]`, in numbered order, without asking, and edit the OpenSpec artifacts (proposal, design doc, spec deltas, tasks) — and, for a touchpoint correction, the phase's Key code touchpoints in the plan file — to reflect it — these are the scrutinizer's own no-downside calls, and you report them in first person. If an `[auto]` option cannot be applied as written — the artifact no longer matches, or the option is ambiguous or looks wrong — do not improvise: leave it unapplied, mark it "not auto-applied" in the summary, and walk it like any untagged finding. Then present the subagent's one-line numbered summary of all findings, marking each auto-applied one "applied" with its `Auto because:` reason, so the user knows the shape of the conversation. Then walk the remaining findings ONE at a time — never dump the full analysis at once; if none remain, skip to Step 4." Verify: `tr '\n' ' ' < commands/scrutinize.md | grep -c 'tagged `\[auto\]`'` = 1, `tr '\n' ' ' < commands/scrutinize.md | grep -c 'not auto-applied'` = 1 and `tr '\n' ' ' < commands/scrutinize.md | grep -c 'Key code touchpoints in the plan file'` = 1.
- [x] 4.2 In `commands/scrutinize.md` Step 4, directly before the line "Update the phase status line to `**Status:** Scrutinized (<change id>)`.", insert a paragraph (wrapped at ~78 columns): "Then append to the end of the phase section a `### Scrutiny notes (<YYYY-MM-DD>)` subsection. Its first sentence reads `<n> findings; auto-applied: <n|none>.`, where `auto-applied` is the count of `[auto]` findings you applied in Step 3; follow it with one paragraph summarising the resolutions, attributing each to the user or, for auto-applied ones, to yourself in first person. With `FINDINGS: 0` it reads `0 findings; auto-applied: none.` and nothing more." Also, in the Step 4 paragraph beginning "After the last item, update the OpenSpec artifacts", change "(proposal, design doc, tasks)" to "(proposal, design doc, spec deltas, tasks)", rewrapping at ~78 columns. Verify: `tr '\n' ' ' < commands/scrutinize.md | grep -c 'auto-applied: <n|none>'` = 1, `grep -c "### Scrutiny notes" commands/scrutinize.md` = 1 and `tr '\n' ' ' < commands/scrutinize.md | grep -c 'update the OpenSpec artifacts (proposal, design doc, spec deltas, tasks)'` = 1.

## 5. Decision protocol, companion docs, version

- [x] 5.1 In `reference/decision-protocol.md`, replace item 5 (from `5. **Do not ask about obvious choices.**` through `driven by the reviewer's \`[auto]\` tag.`) with, keeping the 3-space continuation indent and ~78-column wrap: "5. **Do not ask about obvious choices.** When your confidence is high, the option you would recommend has no con — or its only con is extra work — no other option is equally good, and the action is reversible, act and report it (first person — it is your call, not the user's). Bringing OpenSpec artifacts or specs, docs, or CLAUDE.md in line with the actual code is always such a choice when it is the recommended option and the code, not the doc, is confidently correct. Only steps whose command text adopts this clause use it; today that is `/phaser:review` and `/phaser:scrutinize`, driven by their subagents' `[auto]` tag. An auto-applied review blocker, or an auto-applied fix for a `not-met` criterion, is reported as its own information point: what was chosen, why it was obvious, and the alternatives." Verify: `grep -c "/phaser:scrutinize" reference/decision-protocol.md` = 1 and `tr '\n' ' ' < reference/decision-protocol.md | grep -c 'CLAUDE.md in line with the actual code'` = 1.
- [x] 5.2 In `README.md` "How decisions reach you", replace the paragraph "`review` goes one step further: findings the reviewer marks as having no downside are applied before you are asked anything, and named as applied in the summary — pickers are reserved for real trade-offs." with (wrapped at ~78 columns): "`review` and `scrutinize` go one step further: when the recommended fix has no downside — or only costs more work — and nothing else is as good, it is applied before you are asked anything and named as applied in the summary, and syncing specs and docs to what the code actually does is always applied. An auto-applied review blocker, or a fix that clears a failed acceptance criterion, is spelled out on its own: what was chosen, why, and what was passed over. Pickers are reserved for real trade-offs." Also, in the paragraph beginning "`apply` leaves an `Apply notes` line", insert after "and its source;" the clause " `scrutinize` appends a `Scrutiny notes` subsection with its findings and auto-applied counts;" and rewrap at ~78 columns. Verify: `tr '\n' ' ' < README.md | grep -c 'reserved for real trade-offs'` = 1, `tr '\n' ' ' < README.md | grep -c '`review` and `scrutinize` go one step further'` = 1 and `tr '\n' ' ' < README.md | grep -c 'appends a `Scrutiny notes` subsection'` = 1.
- [x] 5.3 `.claude-plugin/plugin.json` version `0.5.4` → `0.5.5`. Verify: `grep -c '"version": "0.5.5"' .claude-plugin/plugin.json` = 1.
- [x] 5.4 Invariant check: `git diff --quiet HEAD -- commands/plan.md commands/aim.md commands/propose.md commands/apply.md commands/archive.md commands/stun.md agents/implementer.md agents/spec-advisor.md` exits 0. Verify by running it.
