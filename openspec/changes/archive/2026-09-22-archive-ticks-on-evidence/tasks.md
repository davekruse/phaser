## 1. Reviewer reports per criterion

- [x] 1.1 In `agents/reviewer.md` Axis 1, replace the bullet "Are the phase's acceptance criteria met? Test each one against the diff." with D1/D2/D3, wrapped at the file's width: "Judge every acceptance criterion of the phase against the diff and read-only checks, numbered in plan-file order counting every item under `### Acceptance criteria`, ticked ones included, so numbering is stable; report each in the `CRITERIA:` section as `met`, `not-met`, or `unverifiable` (its truth needs a run the diff cannot show: a fixture, a manual session, anything user-run) with a one-line reason. A `not-met` criterion is a finding about the code and forces a `no` verdict." Verify: `grep -c "forces a \`no\` verdict" agents/reviewer.md` = 1 and `grep -c "Test each one against the diff" agents/reviewer.md` = 0.
- [x] 1.2 In `agents/reviewer.md` `## Return` code block, insert between the `2. …` findings line and the `VERDICT:` line: `CRITERIA:` then `1. met | not-met | unverifiable — <one-line reason>` then `2. …`. Verify: `grep -c "^CRITERIA:" agents/reviewer.md` = 1 and `grep -c "unverifiable — <one-line reason>" agents/reviewer.md` = 1.

## 2. Review records it and re-judges after fixes

- [x] 2.1 In `commands/review.md` Step 4, the Review notes line format currently wraps as two lines: `` `**Review notes:** <YYYY-MM-DD>; verdict: <yes|yes-with-deferred>; deferred: `` then `` <list|none>`. ``. Change it to read `` `**Review notes:** <YYYY-MM-DD>; verdict: <yes|yes-with-deferred>; deferred: `` / `` <list|none>; criteria: met <nums|none>; unverifiable <nums|none>`. `` (wrapped as the file wraps) and add the sentence "Copy the numbers from the subagent's `CRITERIA:` section, adjusted by the re-judge below." Verify: `grep -c "unverifiable <nums|none>" commands/review.md` = 1 and `grep -c "adjusted by the re-judge" commands/review.md` = 1.
- [x] 2.2 In `commands/review.md` Step 4, before the two verdict bullets, insert a paragraph per D8: "**Re-judge after fixes.** If the block marks any criterion `not-met`, revisit each after Step 3: re-run a command-shaped one; for a behavioral one, treat it as `met` only if a "Fix now" edit in Step 3 addressed the finding that named it. If no `not-met` criterion remains, the verdict becomes `yes` (or `yes-with-deferred` if anything was deferred); otherwise it stays `no`. Say which criteria were re-judged." Verify: `grep -c "Re-judge after fixes" commands/review.md` = 1.

## 3. Archive ticks on evidence

- [x] 3.1 In `commands/archive.md` Step 3, replace the second bullet (the "Otherwise, put it to the user…" one) with D5, wrapped at the file's width: "Otherwise read the `criteria:` field of the phase's `**Review notes:**` line and find this criterion's number, counting every item under `### Acceptance criteria` in order, ticked ones included (first is 1). Listed under `met`: tick it without asking. Listed under `unverifiable`, listed nowhere, or the Review notes line is absent or has no `criteria:` field: put it to the user via the **decision protocol** in `${CLAUDE_PLUGIN_ROOT}/reference/decision-protocol.md` (read it first), quoting the criterion's text, with options "Verified — tick it (Recommended)" and "Not verified — leave it"; tick only on "Verified"; one criterion per question." Verify: `grep -c "unverifiable" commands/archive.md` ≥ 1, `grep -c "ticked ones included" commands/archive.md` = 1, `grep -c "listed nowhere" commands/archive.md` = 1, `grep -c "Not verified" commands/archive.md` = 1.
- [x] 3.2 In `commands/archive.md` Step 3 closing paragraph, which currently reads "Collect the text of every criterion left unticked (failed command, declined, or otherwise not verified).", append the sentence "Also collect the numbers of every criterion ticked from `met`." Verify: `grep -c "ticked from \`met\`" commands/archive.md` = 1.
- [x] 3.3 In `commands/archive.md` Step 5, the sentence currently wraps as "Then list `Unverified criteria: <each unticked criterion, or" / "none>`. End with:". Insert before "End with:" the sentence "Then list `Ticked on review evidence: <numbers, or none>`." Verify: `grep -c "Ticked on review evidence" commands/archive.md` = 1.

## 4. Companion docs and version

- [x] 4.1 In `README.md`, the archive clause currently wraps as "and deferred items; `archive` re-runs command-shaped acceptance criteria," / "asks you about the rest one at a time, and names any left unticked." Change it to "and deferred items; `archive` re-runs command-shaped acceptance criteria," / "ticks the ones the reviewer verified, asks you only about the ones it could" / "not, and names any left unticked." Verify: `grep -c "reviewer verified" README.md` = 1 and `grep -c "asks you about the rest" README.md` = 0.
- [x] 4.2 `.claude-plugin/plugin.json` version `0.5.1` → `0.5.2`. Verify: `grep -c '"version": "0.5.2"' .claude-plugin/plugin.json` = 1.
- [x] 4.3 Invariant check: `git diff --quiet HEAD -- agents/scrutinizer.md agents/implementer.md agents/spec-advisor.md commands/apply.md commands/plan.md commands/aim.md commands/stun.md commands/propose.md commands/scrutinize.md reference/decision-protocol.md` exits 0. Verify by running it.

## 5. Already done by the main session during propose (not an implementer task)

- [x] 5.1 In `docs/phases/implementation-plan.md`, Phase 2's "Fixture test B" acceptance criterion was reworded to "archive ticks Phase 5's behavioral criterion silently if the reviewer marked it met, asks only if marked unverifiable". Done during propose by the main session, which is the only writer of `docs/phases/`. Verify: `grep -c "asks only if marked unverifiable" docs/phases/implementation-plan.md` = 1.
