## 1. Agents

- [x] 1.1 Create `agents/scrutinizer.md` per design D9 with `model: opus`: Input = change id, plan file, phase; Read = current `commands/scrutinize.md` Step 1 items 1–4 (plan file target phase, OpenSpec artifacts, codebase claims, Key code touchpoints re-verification); Examine = current Step 2 angle lists verbatim (Implementation, Architecture, Unforeseen considerations, Question/Decision classification, discard nitpicks); Return = D8 scrutinizer block. Verify: file exists, frontmatter has `model: opus`, body contains `FINDINGS:` and `SPEC CLAIMS VERIFIED:` and the Isolation paragraph.
- [x] 1.2 Create `agents/implementer.md` per design D9 with `model: sonnet`: Input = change id, base SHA; Read = `openspec instructions apply --change <id> --json` then every `contextFiles` path; Work = tasks in order, rules of engagement copied verbatim from current `commands/apply.md` Step 2 bullets 1–2 (follow the spec exactly; do not improvise), tick `- [ ]` → `- [x]` in `tasks.md` after each task, run tests the tasks name, never commit; Block = on ambiguity/contradiction/collision stop and return `BLOCKED` (never resolve it yourself); Resume = when resumed with a resolution, apply it and continue from the blocked task; Return = D8 implementer block. Verify: file exists, `model: sonnet`, body contains `VERDICT: DONE | BLOCKED` and the Isolation paragraph.
- [x] 1.3 Create `agents/reviewer.md` per design D9 with `model: opus`: Input = change id, base SHA; Read = plan file target phase and acceptance criteria, OpenSpec artifacts, `git diff <base>` plus untracked files (fallback `git diff HEAD` if no base), surrounding code; try `/opsx:verify <id>` first and summarise or report "not available"; Examine = current `commands/review.md` Step 2 Axis 1 and Axis 2 lists verbatim with severity tagging; Return = D8 reviewer block. Verify: file exists, `model: opus`, body contains `VERIFY:`, `FINDINGS:`, `VERDICT: yes | yes-with-deferred | no` and the Isolation paragraph.

## 2. Commands become dispatch-and-walk

- [x] 2.1 Rewrite `commands/scrutinize.md`: remove `disable-model-invocation`; description per D12; delete Step 0 entirely; `argument-hint` gains `[optional: plan file path]`; Step 1 opens with the D2 bullet (note whether the literal `--stun` is in `$ARGUMENTS`, then remove it before parsing anything else) and the D16 bullet (if `$ARGUMENTS` carries a path to an existing `implementation-plan*.md`, use it and skip resolution), then the existing plan-file resolution and target-phase bullets unchanged; Step 2 = dispatch scrutinizer with the D10 prompt (`model: opus`, `subagent_type: phaser:scrutinizer`); Step 3 = if `FINDINGS: 0` skip to Step 4, else one-line numbered summary then walk each finding via the decision protocol (existing Question/Decision option wording kept); Step 4 = fold resolutions into the OpenSpec artifacts and plan file, set `Scrutinized (<id>)`; Step 5 = D11 hand-off to `/phaser:apply <id>` with no `/model sonnet` sentence, including the stop clause (no chaining if the user asked to stop) and passing the plan file path on. Verify: `grep -c "Agent" commands/scrutinize.md` ≥ 1, `grep -c "/clear\|Step 0\|disable-model-invocation" commands/scrutinize.md` = 0, `grep -c "phaser:scrutinizer" commands/scrutinize.md` ≥ 1.
- [x] 2.2 Rewrite `commands/apply.md`: description per D12; drop the "Model note" blockquote; `argument-hint` gains `[optional: plan file path]`; Step 1 opens with the D2 `--stun`-stripping bullet and the D16 plan-file-path bullet, then the existing preflight bullets, base-SHA bullet kept ("keep existing base"); Step 2 = dispatch implementer with the D10 prompt (`model: sonnet`, `subagent_type: phaser:implementer`); Step 3 = relay loop: on `BLOCKED`, dispatch `phaser:spec-advisor` (`model: opus`) with the D10 advisor prompt (change id plus the BLOCKED ON block verbatim); `RESOLVED` → SendMessage the resolution to `phaser:implementer`; `ESCALATE` → decision protocol with the advisor's options, then SendMessage the user's choice; repeat until `DONE`; Step 4 = report TASKS and DEVIATIONS, apply any advisor `SPEC UPDATES NEEDED` items to the OpenSpec artifacts, set `Implemented (<id>, base <sha>)`; Step 5 = D11 hand-off to `/phaser:review <id>` including the stop clause and passing the plan file path on. Verify: `grep -c "SendMessage" commands/apply.md` ≥ 2, `grep -c "/clear\|/model sonnet" commands/apply.md` = 0, `grep -c "phaser:implementer\|phaser:spec-advisor" commands/apply.md` ≥ 2.
- [x] 2.3 Rewrite `commands/review.md`: remove `disable-model-invocation`; description per D12; delete Step 0; `argument-hint` gains `[optional: plan file path]`; Step 1 opens with the D2 `--stun`-stripping bullet and the D16 plan-file-path bullet, then the existing plan-file resolution and target-phase bullets unchanged; Step 2 = dispatch reviewer with the D10 prompt (`model: opus`, `subagent_type: phaser:reviewer`); Step 3 = if `FINDINGS: 0` skip to Step 4, else numbered summary (with severities) then walk each finding via the decision protocol with options Fix now (recommended remedy) / further Fix now variants / Defer / Accept as-is, applying Fix now edits immediately in main; Step 4 = verdict from the block: yes or yes-with-deferred → set `Reviewed (<id>, base <sha>)` and append Review notes; no → state that the status did not advance and why, list must-fix items, no hand-off under either mode; Step 5 = D11 hand-off to `/phaser:archive <id>` (yes verdicts only), with the stop clause and the plan file path passed on. Verify: `grep -c "Agent" commands/review.md` ≥ 1, `grep -c "/clear\|Step 0\|disable-model-invocation" commands/review.md` = 0, `grep -c "phaser:reviewer" commands/review.md` ≥ 1.

## 3. Hand-offs and guards on the untouched commands

- [x] 3.1 `commands/propose.md`: description and Invocation note per D12; `argument-hint` gains `[optional: plan file path]`; Step 1 opens with the D2 `--stun`-stripping bullet (critical here — propose's last argument is free text, so an unstripped `--stun` or path would be folded into the proposal) and the D16 plan-file-path bullet; replace the Step 4 "fresh context required / 1. `/clear`" block with the D11 hand-off to `/phaser:scrutinize <id>`, including the stop clause and the plan file path. Verify: `grep -c "/clear" commands/propose.md` = 0 and `grep -c "\-\-stun" commands/propose.md` ≥ 2.
- [x] 3.2 `commands/archive.md`: description and Invocation note per D12; `argument-hint` gains `[optional: plan file path]`; Step 1 opens with the D2 `--stun`-stripping bullet and the D16 plan-file-path bullet; no hand-off change (its closing line already points at `/phaser:plan` and is the run's Complete report). Verify: description contains "driven by `/phaser:stun`", `grep -c "\-\-stun" commands/archive.md` ≥ 1.
- [x] 3.3 `commands/plan.md`: no functional change; confirm its hand-off still offers `/phaser:propose` without `--stun` (plan is never driven). Verify: `grep -c "\-\-stun" commands/plan.md` = 0.
- [x] 3.4 Repo-wide check: `grep -rn "/clear" commands/ README.md` returns nothing at all (the README's between-phases tip is worded without the literal string, per 5.1), and `grep -rn "fresh context\|Fresh context" commands/` returns nothing. Verify by running both greps.

## 4. New commands

- [x] 4.1 Create `commands/stun.md`: frontmatter `description: "Drive the current phase from Planned to Complete in this session, pausing only for your decisions (run in accept-edits or auto mode for unattended apply)"`, `argument-hint: "[optional: scope] [optional: openspec change id]"`, `model: claude-fable-5-1`, `disable-model-invocation: true`; body per D13 (revised): one-shot dispatcher — resolve the plan file, pick the target phase, read its status line from disk once, invoke the single matching command from the dispatch table in `specs/stun-harness/spec.md` with `--stun` and the resolved plan file path, then print nothing (the running step owns the report). No loop, and no stop-condition messages in stun — state explicitly that the stop conditions are owned by the chained steps (archive reports Complete, a no verdict reports no advance, a user stop request ends the chain). Include a preamble noting that unattended apply wants accept-edits or auto mode. Verify: file exists, frontmatter matches, body contains all five status→command rows each carrying `--stun` and the plan file path, and the string "The plan file is the state machine".
- [x] 4.2 Create `commands/aim.md` per D15: frontmatter `description: "Alias of /phaser:plan — define the next development phase"`, `argument-hint` copied from `commands/plan.md`, `model: claude-fable-5-1`, `disable-model-invocation: true`; body is two lines: "Read `${CLAUDE_PLUGIN_ROOT}/commands/plan.md` and follow it exactly, treating `$ARGUMENTS` as its arguments. This command has no behaviour of its own." Do NOT invoke `/phaser:plan` via the Skill tool — plan carries `disable-model-invocation: true` and is not model-invocable. Verify: `wc -l commands/aim.md` ≤ 12, the body contains no text from plan.md, `grep -c "CLAUDE_PLUGIN_ROOT" commands/aim.md` = 1, and `grep -c "Skill tool" commands/aim.md` = 0; `grep -c "disable-model-invocation" commands/plan.md` = 1 (plan keeps its flag).

## 5. Companion docs and version

- [x] 5.1 `README.md` per D14: table rows for aim and stun, steps 3–5 model column and descriptions, single pin paragraph, fresh-eyes sentence rewritten, a between-phases context tip worded WITHOUT the literal string `/clear` (e.g. "start each phase in a fresh context"), Layout tree with `stun.md`, `aim.md`, `scrutinizer.md`, `implementer.md`, `reviewer.md`. Also document that `/phaser:stun` dispatches one step and the steps chain from there. Verify: `grep -c "/clear" README.md` = 0 and `grep -c "stun" README.md` ≥ 3.
- [x] 5.2 `.claude-plugin/plugin.json` version `0.4.2` → `0.5.0`. Verify: `grep '"version": "0.5.0"' .claude-plugin/plugin.json`.

## 6. Manual verification (user-run after implementation; not implementer tasks)

- [x] 6.1 Fixture: create a throwaway repo with `openspec init --tools claude`, `docs/phases/implementation-plan.md` containing one small Planned phase (e.g. "add a `hello` script that prints the repo name"), install this plugin at 0.5.0. Verify: `/phaser:stun` reaches Complete with each status transition (Proposed, Scrutinized, Implemented with base, Reviewed with base, Complete) visible in the plan file's git history.
- [x] 6.2 In the fixture, seed one ambiguous task in `tasks.md` before apply (two plausible file paths, neither stated). Verify: implementer returns BLOCKED, `phaser:spec-advisor` is consulted, the implementer is resumed via SendMessage and finishes from the blocked task.
- [x] 6.2a In the fixture, answer one scrutinize finding with a request to stop. Verify: the step reports the plan file, phase number and current status line, and does NOT invoke the next command.
- [x] 6.2b Create a second, scoped plan file in the fixture (`docs/phases/implementation-plan-smoke.md`) with one Planned phase and run `/phaser:stun smoke`. Verify: propose receives the scoped plan file path, never asks which scope, and records the phase in the scoped file.
- [x] 6.3 In a session that just ran `/phaser:propose`, run `/phaser:scrutinize <id>` without `/clear`. Verify: findings block arrives from the scrutinizer subagent; no fresh-context warning.
- [x] 6.4 Run `/phaser:aim smoke` in the fixture. Verify: the plan interview starts exactly as `/phaser:plan smoke` would, and no "skill not available" error appears for `/phaser:plan`.
