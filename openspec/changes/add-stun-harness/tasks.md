## 1. Agents

- [ ] 1.1 Create `agents/scrutinizer.md` per design D9 with `model: opus`: Input = change id, plan file, phase; Read = current `commands/scrutinize.md` Step 1 items 1–4 (plan file target phase, OpenSpec artifacts, codebase claims, Key code touchpoints re-verification); Examine = current Step 2 angle lists verbatim (Implementation, Architecture, Unforeseen considerations, Question/Decision classification, discard nitpicks); Return = D8 scrutinizer block. Verify: file exists, frontmatter has `model: opus`, body contains `FINDINGS:` and `SPEC CLAIMS VERIFIED:` and the Isolation paragraph.
- [ ] 1.2 Create `agents/implementer.md` per design D9 with `model: sonnet`: Input = change id, base SHA; Read = `openspec instructions apply --change <id> --json` then every `contextFiles` path; Work = tasks in order, rules of engagement copied verbatim from current `commands/apply.md` Step 2 bullets 1–2 (follow the spec exactly; do not improvise), tick `- [ ]` → `- [x]` in `tasks.md` after each task, run tests the tasks name, never commit; Block = on ambiguity/contradiction/collision stop and return `BLOCKED` (never resolve it yourself); Resume = when resumed with a resolution, apply it and continue from the blocked task; Return = D8 implementer block. Verify: file exists, `model: sonnet`, body contains `VERDICT: DONE | BLOCKED` and the Isolation paragraph.
- [ ] 1.3 Create `agents/reviewer.md` per design D9 with `model: opus`: Input = change id, base SHA; Read = plan file target phase and acceptance criteria, OpenSpec artifacts, `git diff <base>` plus untracked files (fallback `git diff HEAD` if no base), surrounding code; try `/opsx:verify <id>` first and summarise or report "not available"; Examine = current `commands/review.md` Step 2 Axis 1 and Axis 2 lists verbatim with severity tagging; Return = D8 reviewer block. Verify: file exists, `model: opus`, body contains `VERIFY:`, `FINDINGS:`, `VERDICT: yes | yes-with-deferred | no` and the Isolation paragraph.

## 2. Commands become dispatch-and-walk

- [ ] 2.1 Rewrite `commands/scrutinize.md`: remove `disable-model-invocation`; description per D12; delete Step 0 entirely; Step 1 = plan-file resolution and target-phase bullets unchanged; Step 2 = dispatch scrutinizer with the D10 prompt (`model: opus`, `subagent_type: scrutinizer`); Step 3 = if `FINDINGS: 0` skip to Step 4, else one-line numbered summary then walk each finding via the decision protocol (existing Question/Decision option wording kept); Step 4 = fold resolutions into the OpenSpec artifacts and plan file, set `Scrutinized (<id>)`; Step 5 = D11 hand-off to `/phaser:apply <id>` with no `/model sonnet` sentence. Verify: `grep -c "Agent" commands/scrutinize.md` ≥ 1, `grep -c "/clear\|Step 0\|disable-model-invocation" commands/scrutinize.md` = 0.
- [ ] 2.2 Rewrite `commands/apply.md`: description per D12; drop the "Model note" blockquote; Step 1 = existing preflight bullets, base-SHA bullet kept ("keep existing base"); Step 2 = dispatch implementer with the D10 prompt (`model: sonnet`, `subagent_type: implementer`); Step 3 = relay loop: on `BLOCKED`, dispatch `spec-advisor` (`model: opus`) with the BLOCKED ON block verbatim; `RESOLVED` → SendMessage the resolution to the implementer; `ESCALATE` → decision protocol with the advisor's options, then SendMessage the user's choice; repeat until `DONE`; Step 4 = report TASKS and DEVIATIONS, set `Implemented (<id>, base <sha>)`; Step 5 = D11 hand-off to `/phaser:review <id>`. Verify: `grep -c "SendMessage" commands/apply.md` ≥ 2, `grep -c "/clear\|/model sonnet" commands/apply.md` = 0.
- [ ] 2.3 Rewrite `commands/review.md`: remove `disable-model-invocation`; description per D12; delete Step 0; Step 1 = plan-file resolution and target-phase bullets unchanged; Step 2 = dispatch reviewer with the D10 prompt (`model: opus`, `subagent_type: reviewer`); Step 3 = if `FINDINGS: 0` skip to Step 4, else numbered summary (with severities) then walk each finding via the decision protocol with options Fix now (recommended remedy) / further Fix now variants / Defer / Accept as-is, applying Fix now edits immediately in main; Step 4 = verdict from the block: yes or yes-with-deferred → set `Reviewed (<id>, base <sha>)` and append Review notes; no → leave status, list must-fix items, no hand-off; Step 5 = D11 hand-off to `/phaser:archive <id>` (yes verdicts only). Verify: `grep -c "Agent" commands/review.md` ≥ 1, `grep -c "/clear\|Step 0\|disable-model-invocation" commands/review.md` = 0.

## 3. Hand-offs and guards on the untouched commands

- [ ] 3.1 `commands/propose.md`: description and Invocation note per D12; replace the Step 4 "fresh context required / 1. `/clear`" block with the D11 hand-off to `/phaser:scrutinize <id>`. Verify: `grep -c "/clear" commands/propose.md` = 0 and `grep -c "\-\-stun" commands/propose.md` ≥ 1.
- [ ] 3.2 `commands/archive.md`: description and Invocation note per D12; no hand-off change (its closing line already points at `/phaser:plan`). Verify: description contains "driven by `/phaser:stun`".
- [ ] 3.3 `commands/plan.md`: no functional change; confirm its hand-off still offers `/phaser:propose` without `--stun` (plan is never driven). Verify: `grep -c "\-\-stun" commands/plan.md` = 0.
- [ ] 3.4 Repo-wide check: `grep -rn "/clear\|fresh context\|Fresh context\|cold read of" commands/ README.md` returns nothing except README prose rewritten in 5.1. Verify by running the grep.

## 4. New commands

- [ ] 4.1 Create `commands/stun.md`: frontmatter `description: "Drive the current phase from Planned to Complete in this session, pausing only for your decisions (run in accept-edits or auto mode for unattended apply)"`, `argument-hint: "[optional: scope] [optional: openspec change id]"`, `model: claude-fable-5-1`, `disable-model-invocation: true`; body per D13 with the dispatch table from `specs/stun-harness/spec.md` and all four stop conditions, each with the exact message to print. Verify: file exists, frontmatter matches, body contains all five status→command rows and the string "The plan file is the state machine".
- [ ] 4.2 Create `commands/aim.md`: frontmatter `description: "Alias of /phaser:plan — define the next development phase"`, `argument-hint` copied from `commands/plan.md`, `model: claude-fable-5-1`, `disable-model-invocation: true`; body is two lines: "Invoke `/phaser:plan $ARGUMENTS` via the Skill tool now. This command has no behaviour of its own." Verify: `wc -l commands/aim.md` ≤ 12 and the body contains no text from plan.md.

## 5. Companion docs and version

- [ ] 5.1 `README.md` per D14: table rows for aim and stun, steps 3–5 model column and descriptions, single pin paragraph, fresh-eyes sentence rewritten, tip to `/clear` between phases, Layout tree with `stun.md`, `aim.md`, `scrutinizer.md`, `implementer.md`, `reviewer.md`. Verify: `grep -c "/clear" README.md` = 1 (the between-phases tip only) and `grep -c "stun" README.md` ≥ 3.
- [ ] 5.2 `.claude-plugin/plugin.json` version `0.4.2` → `0.5.0`. Verify: `grep '"version": "0.5.0"' .claude-plugin/plugin.json`.

## 6. Manual verification (user-run after implementation; not implementer tasks)

- [ ] 6.1 Fixture: create a throwaway repo with `openspec init --tools claude`, `docs/phases/implementation-plan.md` containing one small Planned phase (e.g. "add a `hello` script that prints the repo name"), install this plugin at 0.5.0. Verify: `/phaser:stun` reaches Complete with each status transition (Proposed, Scrutinized, Implemented with base, Reviewed with base, Complete) visible in the plan file's git history.
- [ ] 6.2 In the fixture, seed one ambiguous task in `tasks.md` before apply (two plausible file paths, neither stated). Verify: implementer returns BLOCKED, spec-advisor is consulted, the implementer is resumed via SendMessage and finishes from the blocked task.
- [ ] 6.3 In a session that just ran `/phaser:propose`, run `/phaser:scrutinize <id>` without `/clear`. Verify: findings block arrives from the scrutinizer subagent; no fresh-context warning.
- [ ] 6.4 Run `/phaser:aim smoke` in the fixture. Verify: the plan interview starts exactly as `/phaser:plan smoke` would.
