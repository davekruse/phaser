## 1. Implementer contract and deviations

- [x] 1.1 In `agents/implementer.md`, replace the `## Block` section body with D1: before returning `BLOCKED`, read `proposal.md`, `design.md` and every file under `specs/` for the answer; if exactly one resolution follows, apply it and add a `DEVIATIONS` line; return `BLOCKED` only when no artifact answers or artifacts contradict each other. Keep the closing sentence "spec gaps at this stage are findings, not license for you to architect." Verify: `grep -c "exactly one resolution" agents/implementer.md` = 1 and `grep -c "contradict each other" agents/implementer.md` = 1.
- [x] 1.2 In `agents/implementer.md`, rewrite the `## Resume` section's deviation clause to "note it under DEVIATIONS with its source (advisor or user)", dropping "if it was a user/advisor-approved deviation from the literal task text". Verify: `grep -c "literal task text" agents/implementer.md` = 0.
- [x] 1.3 In `agents/implementer.md` `## Return`, replace the `DEVIATIONS:` placeholder lines with D2's shape: `- <task id>: <what was done> — source: <artifact § section | advisor | user>` and the line `(none only when every task was executed exactly as its text reads)`. Verify: `grep -c "source: <artifact" agents/implementer.md` = 1 and `grep -c "exactly as its text reads" agents/implementer.md` = 1.

## 2. Apply records deviations

- [x] 2.1 In `commands/apply.md` Step 4, after "Report the implementer's `TASKS` count and every line under `DEVIATIONS`.", add a paragraph per D3: insert an `**Apply notes:** <YYYY-MM-DD>; deviations: none` or `**Apply notes:** <YYYY-MM-DD>; deviations: <n> — <line>; <line>` line directly after the target phase's `**Defined:**` line, replacing any existing `**Apply notes:**` line for that phase. Verify: `grep -c "Apply notes" commands/apply.md` ≥ 2.
- [x] 2.2 In `commands/apply.md` Step 4, reword the `ESCALATE` follow-up sentence so the spec update "records the decision the user made" stays and add "attribute it to the user, never to yourself" per D8. Verify: `grep -c "never to yourself" commands/apply.md` = 1.

## 3. Reviewer verify and checkbox rules

- [x] 3.1 In `agents/reviewer.md` Axis 1, replace the first bullet (the `/opsx:verify` one) with D5: run `openspec validate <id> --type change` via Bash; summarise on the `VERIFY` line as `openspec validate — valid` or `openspec validate — <n> issue(s): <first line of each>`; "not available" only when the `openspec` binary is absent; its report is input, not verdict. Verify: `grep -c "opsx:verify" agents/reviewer.md` = 0 and `grep -c "openspec validate <id> --type change" agents/reviewer.md` = 1.
- [x] 3.2 In `agents/reviewer.md` `## Return`, change the `VERIFY:` placeholder to `VERIFY: openspec validate — valid | <n> issue(s): <summary> | not available`. Verify: `grep -c "opsx:verify" agents/reviewer.md` = 0 (still) and `grep -c "VERIFY: openspec validate" agents/reviewer.md` = 1.
- [x] 3.3 In `agents/reviewer.md` Axis 1, after the "Are the phase's acceptance criteria met?" bullet, add D6: "An unticked acceptance checkbox in the plan file is never a finding — archive ticks them at close-out. An unmet criterion is a finding about the code." Verify: `grep -c "never a finding" agents/reviewer.md` = 1.

- [x] 3.4 In `commands/review.md` Step 4, replace "append a short \"Review notes\" line to the phase section recording deferred items, if any" with D3: insert `**Review notes:** <YYYY-MM-DD>; verdict: <yes|yes-with-deferred>; deferred: <list|none>` directly after the phase's `**Apply notes:**` line (or after `**Defined:**` if there is none), replacing any existing `**Review notes:**` line for that phase. Verify: `grep -c "verdict: <yes|yes-with-deferred>" commands/review.md` = 1 and `grep -c "short \"Review notes\" line" commands/review.md` = 0.

## 4. False-claim recommendation rule

- [x] 4.1 In `agents/scrutinizer.md`, add to the `## Return` section (before the code block) the sentence: "When a finding is that an artifact states something false about the codebase, the option marked recommended is the one that corrects the artifact — never 'leave as-is'." Verify: `grep -c "corrects the artifact" agents/scrutinizer.md` = 1.
- [x] 4.2 Add the same sentence, with "remedy" for "option", to `agents/reviewer.md` `## Return` before its code block. Verify: `grep -c "corrects the artifact" agents/reviewer.md` = 1.

## 5. Decision protocol

- [x] 5.1 In `reference/decision-protocol.md` item 2, after "one option per candidate (usually 2–3)", add: "or `multiSelect: true` when the candidates are not mutually exclusive, such as a list of requirements". Verify: `grep -c "multiSelect" reference/decision-protocol.md` = 1.
- [x] 5.2 Append item 4 to `reference/decision-protocol.md`: "**Attribute the outcome to the user.** In hand-offs, summaries, plan-file notes and OpenSpec artifacts, say what the user chose — never 'I resolved' or 'I decided' for a choice that went through this protocol. First person is for choices you made without asking." Verify: `grep -c "Attribute the outcome" reference/decision-protocol.md` = 1.

## 6. Archive ticks acceptance criteria

- [x] 6.1 In `commands/archive.md`, insert a new `## Step 3: Tick acceptance criteria` between the archive step and the status update, renumbering the later steps to 4 and 5, with D4's rule: for each unticked `- [ ]` under the phase's `### Acceptance criteria`: if it contains a backticked shell command and a stated expected result, run it from the repo root and tick on match (skip and report any command that would write); otherwise put it to the user via the decision protocol in `${CLAUDE_PLUGIN_ROOT}/reference/decision-protocol.md` (read it first), quoting the criterion, options "Verified — tick it (Recommended)" and "Not verified — leave it", one criterion per question, ticking on "Verified"; collect the text of every criterion left unticked. Verify: `grep -c "Step 3: Tick acceptance criteria" commands/archive.md` = 1, `grep -c "Step 4: Record completion" commands/archive.md` = 1, `grep -c "Step 5: Hand off" commands/archive.md` = 1, `grep -c "Not verified" commands/archive.md` = 1.
- [x] 6.2 In `commands/archive.md` Step 5 (Hand off), add before the closing blockquote: "Then list `Unverified criteria: <each unticked criterion, or none>`." Verify: `grep -c "Unverified criteria" commands/archive.md` = 1.

## 7. Plan interview and hand-off

- [x] 7.1 In `commands/plan.md` Step 2, keep the "**Always open with a question.**" paragraph verbatim; replace the paragraph beginning "Interview the user conversationally (a few questions at a time" with D9: after the opening answer, whenever candidate answers can be proposed for goal wording, requirements, out-of-scope items, dependencies, acceptance criteria or a phase split, present them through the decision protocol in `${CLAUDE_PLUGIN_ROOT}/reference/decision-protocol.md` (read it first), one AskUserQuestion per message, `multiSelect` for list items; ask in prose only when no candidates can be proposed. Keep the bullet list of the five things to articulate. Verify: `grep -c "a few questions at a time" commands/plan.md` = 0 and `grep -c "decision-protocol.md" commands/plan.md` = 1 and `grep -c "multiSelect" commands/plan.md` = 1.
- [x] 7.2 In `commands/plan.md` Step 3, change "Present a draft of the phase section to the user for confirmation, then append it" to present the draft in text and confirm with a single-select picker (Append as drafted (Recommended) / Edit first) before appending. Verify: `grep -c "Append as drafted" commands/plan.md` = 1.
- [x] 7.3 Replace `commands/plan.md` Step 4 with D10: confirm the phase is saved, then end with the blockquote `**Next step:** /phaser:stun — drive Phase N from Planned to Complete, pausing only for your decisions.` and no Skill invocation. Verify: `sed -n '/## Step 4/,$p' commands/plan.md | grep -c "phaser:propose"` = 0, `grep -c "Skill" commands/plan.md` = 0, `grep -c "phaser:stun" commands/plan.md` ≥ 1.
- [x] 7.4 Confirm `commands/aim.md` is byte-identical to its committed version. Verify: `git diff --quiet HEAD -- commands/aim.md` exits 0.

## 8. Companion docs and version

- [x] 8.1 In `README.md`, replace the paragraph starting "`plan` is the exception:" with: "`plan` opens with one prose question — what should this phase accomplish? — and follows the protocol from there, with multi-select pickers for lists like requirements and out-of-scope items. When the phase is saved it names `/phaser:stun` as the next step." Keep the following sentence about `/phaser:plan ats`. Verify: `grep -c "is the exception" README.md` = 0 and `grep -c "names \`/phaser:stun\`" README.md` = 1.
- [x] 8.2 In `README.md`, in the plan-file paragraph, after "`Implemented` also records the base commit SHA so `review` can diff the whole phase, including anything committed along the way.", add: "`apply` leaves an `Apply notes` line recording every deviation from the task text and its source; `review` leaves a `Review notes` line with its verdict and deferred items; `archive` re-runs command-shaped acceptance criteria, asks you about the rest one at a time, and names any left unticked." Verify: `grep -c "Apply notes" README.md` = 1.
- [x] 8.3 In `README.md` workflow table, change the archive row's description to "`/opsx:archive` + tick acceptance criteria + mark phase Complete". Verify: `grep -c "tick acceptance criteria" README.md` = 1.
- [x] 8.4 `.claude-plugin/plugin.json` version `0.5.0` → `0.5.1`. Verify: `grep -c '"version": "0.5.1"' .claude-plugin/plugin.json` = 1.
- [x] 8.5 Repo-wide checks: `grep -rn "opsx:verify" agents/ commands/` returns nothing; `git diff --quiet HEAD -- commands/aim.md commands/scrutinize.md commands/stun.md agents/spec-advisor.md` exits 0 (propose.md dropped from this list at review: its invocation guard was reworded per finding 5, the user's choice). Verify by running both.
